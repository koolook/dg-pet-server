meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  artifacts:
    '*': /app
  command: npm run build
run:
  command: npm run start
  persistenceMount: /app/uploaded
  containerPort: 4000
