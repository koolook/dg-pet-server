---
version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  additionalCommands: npm run build
  artifacts:
    '*': /
run:
  command: npm run start
  persistenceMount: /app/uploaded
  containerPort: 4000
